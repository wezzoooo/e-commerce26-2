export interface SubCategoryI {
    _id: string
    name: string
    slug: string
    category: string
    createdAt: string
    updatedAt: string
}

export type SubCategoryIdType = {
    subCategoryId: string
}